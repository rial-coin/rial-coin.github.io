import React from "react";

export const WalletInfo: React.FC = () => {
  // Temporarily return null to prevent TonConnect context errors
  // This component will only work when the entire Navbar is wrapped in TonConnectUIProvider
  return null;
};
